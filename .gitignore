# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# ── Environment ──
qas.env
sit.env
prod.env
.env
.env.*
!.env.example
!.env.*.example
venv/
.venv/

# ── OS ──
.DS_Store
Thumbs.db
*.swp
*.swo

# ── Local config / agent runtime ──
.zcode/
.agents/
.zcode-*

# ── Test artifacts ──
.playwright-mcp/

# ── Secrets (per-directory) ──
docker/.env
docker/.env.owui
docker/.env.litellm

# ── Local data ──
data/open-webui/
data/n8n/

# ── POC exports (regenerable) ──
data/open_webui_poc.dump
data/open_webui_poc.sql

# ── IDE ──
.vscode/
.idea/
*.code-workspace
data/ingestion/
node_modules/

# ── Generated files ──
*.pptx
*.pdf

# ── Image files (except screenshots) ──
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.bmp
*.ico
*.svg
# Allow screenshots (needed by Docker build)
!screenshots/
!screenshots/*

# Local trial keys (never commit)
docker/.procurement-trial-key.txt

docker/.entchat-qas-vkey.txt
