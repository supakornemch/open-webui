# ⚠️ DEPRECATED — Use docker-compose.local.yml instead (supports custom OWUI image + SQLite)
#
# EnterpriseChat — Full Local POC Stack
# Open WebUI + LiteLLM (Azure Foundry proxy)
#
# Usage:
#   docker compose -f config/docker-compose.local.yml up -d --build
#
# Services:
#   Open WebUI:  http://localhost:3000
#   LiteLLM API: http://localhost:4000
#   LiteLLM UI:  http://localhost:4000/ui
